#!/usr/bin/env python

import sys

def main():
    if name == 'Guido':
        print repeeeet(name) + '!!!' # program will compile and work until the name happens to be Guido
    else:
        print repeat(name)

if __name__ == '__main__':
    main()
